# This service crawls and scores. It serves no HTTP and owns no schema, so
# there is no web framework, no migration tool and no auth stack here — those
# live in the tender-intelligence app.

# Database
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# Crawler
# Scrapy 2.13 replaced Spider.start_requests() with an async start(), and 2.17
# removed the old method from the base class entirely -- a spider defining only
# start_requests() there crawls nothing without raising. Do not drop below 2.13.
scrapy>=2.13,<3
lxml==5.2.2
# Only needed for `run_crawler.py --schedule`, the fallback for platforms with
# no cron scheduler of their own.
apscheduler==3.10.4

# Scoring
scikit-learn==1.5.1
# Claude reads the shortlisted notices and returns the structured assessment.
anthropic>=0.69,<1
