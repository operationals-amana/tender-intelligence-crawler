{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "API service. Deploy this repo as a Railway service with a Postgres plugin attached; set DATABASE_URL, JWT_SECRET, CORS_ORIGINS and AUTO_CREATE_TABLES=false in the service variables. The entrypoint applies migrations before uvicorn starts.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "./docker-entrypoint.sh api",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "numReplicas": 1
  }
}
